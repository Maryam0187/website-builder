[build]
builder = "NIXPACKS"
# Nixpacks already runs `npm ci` in install — only build here
buildCommand = "npm run build"

[deploy]
startCommand = "bash scripts/railway-start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
